apiVersion: v1
kind: Service
metadata:
  name: fashionista-frontend
  namespace: fashionista
spec:
  selector:
    app: fashionista-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
 